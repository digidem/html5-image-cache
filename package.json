{
  "name": "image-cache",
  "version": "0.0.1",
  "description": "Caches and images on a page in persistent storage",
  "main": "index.js",
  "repository": { 
    "type" : "git",
    "url" : "http://github.com/digidem/html5-image-cache.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gregor MacLennan",
  "license": "MIT",
  "dependencies": {
    "domready": "^1.0.5",
    "inline-worker": "0.0.0"
  },
  "devDependencies": {
    "browserify": "^5.9.1",
    "uglify-js": "^2.4.15"
  }
}
